% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transferencias.R
\name{get_tc_por_municipio}
\alias{get_tc_por_municipio}
\alias{get_tc_by_municipality}
\title{Get constitutional transfers by municipality}
\usage{
get_tc_por_municipio(
  p_estado = NULL,
  p_municipio = NULL,
  p_ano = NULL,
  p_mes = NULL,
  p_transferencia = NULL,
  p_sn_detalhar = NULL,
  page_size = 1000L,
  max_rows = Inf,
  use_cache = TRUE,
  verbose = FALSE
)

get_tc_by_municipality(state_code = NULL, municipality = NULL,
  year = NULL, month = NULL, transfer_type = NULL, detailed = NULL,
  page_size = 1000L, max_rows = Inf, use_cache = TRUE, verbose = FALSE)
}
\arguments{
\item{p_estado}{State code(s) from \code{\link[=get_tc_estados]{get_tc_estados()}}. Accepts a vector
or colon-separated string. Optional.}

\item{p_municipio}{Municipality code(s) from \code{\link[=get_tc_municipios]{get_tc_municipios()}}.
Accepts a vector or colon-separated string. Optional.}

\item{p_ano}{Year(s). Accepts a vector or colon-separated string.
Optional.}

\item{p_mes}{Month(s). Accepts a vector or colon-separated string.
Optional.}

\item{p_transferencia}{Transfer type code(s) from
\code{\link[=get_tc_transferencias]{get_tc_transferencias()}}. Accepts a vector or colon-separated string.
Optional.}

\item{p_sn_detalhar}{Character. Set to \code{"sim"} (the documented value;
\code{"S"} also works) to break every transfer into its components (for
example \code{FUNDEB - FPM}, \code{FUNDEB - ICMS}). Optional.
\code{\link[=get_tc_por_municipio_detalhe]{get_tc_por_municipio_detalhe()}} returns the same breakdown with the
three ten-day instalments.}

\item{page_size}{Integer. Rows requested per page (the server pages
this endpoint; 10 rows by default on the server side, 1000 here).}

\item{max_rows}{Integer. Stop after this many rows. Default \code{Inf}.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL being
called. Useful for debugging or testing in a browser. Defaults to
\code{getOption("tesouror.verbose", FALSE)}.}

\item{state_code}{State code(s) from \code{\link[=get_tc_estados]{get_tc_estados()}}. Accepts a
vector or colon-separated string. \strong{Treasury codes}, not IBGE.
Optional. Maps to \code{p_estado}.}

\item{municipality}{Municipality code(s) from \code{\link[=get_tc_municipios]{get_tc_municipios()}}.
Accepts a vector or colon-separated string. \strong{Treasury codes}.
Optional. Maps to \code{p_municipio}.}

\item{year}{Year(s). Accepts a vector or colon-separated string.
Optional. Maps to \code{p_ano}.}

\item{month}{Month(s) (1-12). Accepts a vector or colon-separated
string. Optional. Maps to \code{p_mes}.}

\item{transfer_type}{Transfer type code(s) from
\code{\link[=get_tc_transferencias]{get_tc_transferencias()}}. Accepts a vector or colon-separated
string. Optional. Maps to \code{p_transferencia}.}

\item{detailed}{Character. Set to any value to include detail.
Optional. Maps to \code{p_sn_detalhar}.}
}
\value{
A \link[tibble:tibble]{tibble} with transfer data by municipality
(\code{uf}, \code{ano}, \code{mes}, \code{transferencia}, \code{codigo_siafi}, \code{co_ibge},
\code{municipio}, \code{valor}).
}
\description{
Retrieves constitutional transfer data for municipalities within states.
}
\details{
All codes are \strong{internal Treasury codes} (not IBGE). Use the dictionary
functions to look them up: \code{\link[=get_tc_estados]{get_tc_estados()}} for state codes,
\code{\link[=get_tc_municipios]{get_tc_municipios()}} for municipality codes, and
\code{\link[=get_tc_transferencias]{get_tc_transferencias()}} for transfer type codes.

Multi-value parameters accept either a colon-separated string
(\code{"1:2:3"}) or an R vector (\code{c(1, 2, 3)}).

\code{get_tc_by_municipality()} is an English alias.

The municipal endpoints are paginated: every page is fetched and the
rows stacked. If a page after the first fails, the rows already fetched
are returned with \code{attr(x, "partial") = TRUE} and
\code{attr(x, "last_page_error")}.
}
\examples{
\dontrun{
# Step 1: look up codes (Treasury codes, NOT IBGE)
estados <- get_tc_estados()
pe_code <- estados$codigo[estados$nome == "Pernambuco"]
municipios <- get_tc_municipios(p_uf = pe_code)
recife_code <- municipios$codigo[municipios$nome == "Recife"]

# Step 2: query (pass vector or string)
tc <- get_tc_por_municipio(
  p_estado = pe_code,
  p_municipio = recife_code,
  p_ano = 2023
)
}
}
\seealso{
Other Transferencias:
\code{\link[=get_tc_estados]{get_tc_estados()}},
\code{\link[=get_tc_municipios]{get_tc_municipios()}},
\code{\link[=get_tc_por_estados]{get_tc_por_estados()}},
\code{\link[=get_tc_por_estados_detalhe]{get_tc_por_estados_detalhe()}},
\code{\link[=get_tc_por_municipio_detalhe]{get_tc_por_municipio_detalhe()}},
\code{\link[=get_tc_transferencias]{get_tc_transferencias()}}
}
\concept{Transferencias}
