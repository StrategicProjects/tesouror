% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/siope.R
\name{get_siope_indicadores}
\alias{get_siope_indicadores}
\alias{get_siope_indicators}
\title{Get SIOPE education indicators}
\usage{
get_siope_indicadores(
  ano,
  periodo,
  uf,
  tipo = NULL,
  use_cache = TRUE,
  verbose = FALSE,
  page_size = 1000L,
  max_rows = Inf,
  filter = NULL,
  orderby = NULL,
  select = NULL
)

get_siope_indicators(year, period, state, type = NULL, use_cache = TRUE,
  verbose = FALSE,
  page_size = 1000, max_rows = Inf,
  filter = NULL, orderby = NULL, select = NULL)
}
\arguments{
\item{ano}{Integer. Year of the data (e.g., \code{2023}). \strong{Required}.}

\item{periodo}{Integer. Bimester period (1-6). \strong{Required}.}

\item{uf}{Character. State abbreviation (e.g., \code{"PE"}). \strong{Required}.}

\item{tipo}{Character. Convenience filter on the entity type, applied
\strong{server-side} via the \code{DS_TIPO} column so you don't download both the
state and all its municipalities. Accepts \code{"estado"}/\code{"uf"} (state) or
\code{"municipio"}/\code{"municipios"} (municipalities), accent- and
case-insensitive. Combined with \code{filter} via \code{and} when both are given.
Defaults to \code{NULL} (no type filter). \code{type} is the English alias.}

\item{use_cache}{Logical. If \code{TRUE} (default), uses an in-memory cache.}

\item{verbose}{Logical. If \code{TRUE}, prints the full API URL. Defaults to
\code{FALSE}. Set globally with \code{options(tesouror.verbose = TRUE)}.}

\item{page_size}{Integer. Rows per page for OData pagination. Defaults
to \code{1000}.}

\item{max_rows}{Numeric. Maximum rows to return. Defaults to \code{Inf}.}

\item{filter}{Character. OData \verb{$filter} expression to narrow results
\strong{on the server} (much faster than downloading everything). Uses
OData syntax (e.g., \code{"NOM_MUNI eq 'Recife'"},
\code{"COD_MUNI eq 2611606"}). Combine with \code{and}/\code{or}. Column names
must use the \strong{original API names} (uppercase), not the snake_case
cleaned names. To discover valid names, run a \code{max_rows = 1} query
and use \code{toupper(names(result))}. Optional.}

\item{orderby}{Character. OData \verb{$orderby} expression to sort results
(e.g., \code{"NOM_MUNI asc"}, \code{"NUM_POPU desc"}). Uses original API
column names (uppercase). Optional.}

\item{select}{Character vector. Column names to return (reduces payload
size). Uses original API column names (e.g.,
\code{c("NOM_MUNI", "VAL_DECL")}). If a column name is invalid the API
returns HTTP 400. Optional.}

\item{year}{Integer. Year. \strong{Required}. Maps to \code{ano}.}

\item{period}{Integer. Bimester (1-6). \strong{Required}. Maps to \code{periodo}.}

\item{state}{Character. State abbreviation. \strong{Required}. Maps to \code{uf}.}

\item{type}{Character. English alias for \code{tipo}: \code{"state"}/\code{"uf"} or
\code{"municipality"}/\code{"municipalities"}. Optional, defaults to \code{NULL}.}
}
\value{
A \link[tibble:tibble]{tibble} with 13 columns including indicator
codes and values.
}
\description{
Retrieves education spending indicators such as percentage of revenue
applied to education, FUNDEB compliance, etc.
}
\details{
\code{get_siope_indicators()} is an English alias.
}
\examples{
\dontrun{
ind <- get_siope_indicadores(ano = 2023, periodo = 6, uf = "PE")
}
}
\seealso{
Other SIOPE:
\code{\link[=get_siope_dados_gerais]{get_siope_dados_gerais()}},
\code{\link[=get_siope_despesas]{get_siope_despesas()}},
\code{\link[=get_siope_despesas_funcao]{get_siope_despesas_funcao()}},
\code{\link[=get_siope_info_complementares]{get_siope_info_complementares()}},
\code{\link[=get_siope_receitas]{get_siope_receitas()}},
\code{\link[=get_siope_remuneracao]{get_siope_remuneracao()}},
\code{\link[=get_siope_responsaveis]{get_siope_responsaveis()}}
}
\concept{SIOPE}
